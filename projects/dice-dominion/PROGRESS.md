# 중간 기록 — dice-dominion

- 프로젝트: DICE DOMINION (`dice-dominion`)
- 스냅샷 시점: **2026-08-17 22:10** (게임 저장소 `9c5b5f4` · `ART_STUDIO_LIAISON.md` 22:00 판과 대조)
- 문서 성격: **진행 상황 스냅샷.** 정본이 아니다

<!--
이 프로젝트는 게임 저장소가 아트 문서를 이미 갖고 있다. 정본은 그쪽이다 —
ART_DIRECTION.md · src/art/spec.ts · ASSET_PIPELINE.md.
판단과 그 이유는 reviews/REVIEW_LOG_ivory-frame.md 가 갖는다.
여기 있는 것은 «지금 어디까지 왔고 무엇이 막혀 있는가» 하나뿐이다.
-->

---

## 1. 지금 어디까지 왔나

**색 실측 → 토큰 확정 → C1 패널 부품 3벌 수령 → 조립 2회 → 재발주 대기.**

```
아이보리 색 토큰 실측     Review 01   완료 — 의미 토큰 16개
판 제작 방식 · 색 변형     Review 02   완료 — B1·B4·B5 디렉터 결정
C1 패널 부품 3벌 조립      Review 03   완료 — 접합면 문제 진단
조립기 수정 후 재조립      Review 04   완료 — 세트 순위 뒤집힘 · 재발주 결정
실행 화면 · 가문 4색       Review 05   완료 — 색 확정 (디렉터 위임)
색값 게임 반영             게임 저장소  완료 — `engine.ts:61` · `spec.ts:33` · 테스트로 결합 고정
C1 재발주                 —           **대기.** 발주서 v2 미작성 ← **지금 병목은 Studio 쪽이다**
```

**승인 자산은 아직 없다.** 부품 3벌은 전부 `CANDIDATE` 유지 — 반려가 아니라 다음 후보의 재료다.

**게임 쪽이 22:00 에 「우리가 art-studio 에 막고 있는 것은 없다」고 적었다**
(`ART_STUDIO_LIAISON.md` §2). 2차 프레임 부품은 디렉터가 만들고 있고,
**그 부품이 도착하기 전에 발주서 v2 가 나가 있어야 한다** — 조건 없이 뽑으면 1차와 같은 것이 온다.

---

## 2. 확정된 것

| | 값 · 결정 | 근거 |
|---|---|---|
| 톤 | **아이보리.** 순백이 아니라 따뜻한 회색 (`panel.bg #e4ddd8`) | 디렉터 결정 · Review 01 |
| 화풍 기준 | `11_44_21 (2)` 전투 화면 한 장. **매 생성마다 앵커로 첨부** | 발주서 |
| 앵커 세 장의 분담 | **전투 화면 = 톤·화풍 최상위 기준.** 왕성 판·남극 판은 **구도만** — 두 판 시안의 어두운 남색·무거운 금장은 **가져오지 않는다**. 판은 전투보다 한 단계 가볍다 | 게임 `ART_DIRECTION.md` §2 |
| 판 제작 (B1) | **Unity 3D 링 + 평면 타일 텍스처.** 중앙 디오라마·외곽 배경만 그림 | 디렉터 결정 · Review 02 |
| 초상 (B4) | 영웅당 **한 벌.** 판에서는 축소본 | 디렉터 결정 · Review 02 |
| 애니메이션 (B5) | 이번 마일스톤은 **정지만** | 디렉터 결정 · Review 02 |
| 가문색 (B3) | **코드 틴트로 성립.** 발주 조건 — 「가문색은 의상과 받침대에만. 머리·눈·피부·금속에 쓰지 않는다」 | 시험 완료 · Review 02 |
| **가문 4색 값** | `house.red #93544c` · `blue #416491` · `green #6a934c` · `purple #695794`<br>`on.panel.header #ece3da` · `on.accent.blue #edeae6` | **디렉터 위임에 따른 확정** · Review 05 |
| 소유 표시 | **(a) 지금 그대로 둔다.** 소유는 색이 아니라 **조형**으로 읽힌다 — 3D 링의 시설물이 그 역할을 겸한다 | Review 05 · 게임 반영 완료(무작업) |
| **화풍 · 엔진** | 게임이 **HD-2D 도트를 철회**하고 콘셉트 아트 화풍 그대로 확정. 엔진은 **Unity**, 브라우저 구현은 검증된 사양서로 남는다 | 게임 `ART_DIRECTION.md` (2026-08-17 개정) — **정본은 그쪽** |

> **적·녹은 유도값이다.** 판 시안에서 색상만 취하고 실측된 보라·청의 명도·채도를 입혔다.
> 같은 변환이 보라·청의 실측값을 채널당 11~13 차이로 재현한다.
> 실제 실행 화면에 네 색을 나란히 놓고 확정했다.

---

## 3. 열려 있는 것

| | 무엇 | 언제 닫히나 |
|---|---|---|
| **B2** | 기준 캔버스와 판 위 표시 크기 | 첫 한 바퀴를 돌린 화면에서 눈으로. **E1(패널) 한 바퀴가 그 한 바퀴다** — `spec.ts` 의 512×512 도 Unity 씬(Phase C)도 여기에 걸려 있다 |
| **C1** | 패널 프레임 — 「연속된 테두리」를 아직 못 만든다 | 접합면 조건을 붙인 재발주 뒤 |
| B3 검증 | 위 발주 조건이 지켜진 첫 미니어처 후보 | 미니어처 착수 시 |
| 용병 가문색 24장 | 색 확정으로 **열렸을 뿐** | B3 검증이 통과하면 사라지고, 실패하면 24장이 되살아난다 |
| 적색 채도 보정 | 유도값인 적·녹이 실물에서 견디는가 | 첫 미니어처 후보 위에서 |

**C1 이 막힌 원인은 취향이 아니라 발주서에 없던 규정이다** — 띠의 좌우 끝과 모서리의 안쪽 끝이
어떤 단면으로 끝나야 하는지 아무도 말하지 않았고, 그래서 셋 다 「완결된 장식 하나」로 그려 왔다.

---

## 4. 다음 한 걸음

1. **발주서 v2** ← **하나뿐인 실제 다음 칸.** 접합면 조건 추가.
   「띠의 좌우 끝은 잘린 단면으로 끝날 것 · 모서리의 안쪽 두 끝은 띠와 같은 단면으로 끝나 캔버스
   가장자리에 닿을 것」. 참조로 **모서리는 `set_3`, 띠는 `set_1`** 을 첨부한다
2. 채팅방 폴더 재구성 → 3벌 재수령 → 재조립 → **Review 06**
3. `banner` · `button` 은 `panel` 이 화면을 통과한 뒤 같은 문안으로 발주한다

> **~~색값을 게임 세션에 전달~~ — 완료(2026-08-17).** `engine.ts:61` · `spec.ts:33` 이 같은
> 네 문자열을 갖고, `pipeline.test.ts` 가 둘의 어긋남을 오류로 잡는다.
> 어두운 바탕 글자색 두 개와 `--on-primary` 실측 통일도 함께 들어갔다.

> **Review 번호 주의** — Review 04 가 예고한 「재조립 → Review 05」의 자리를
> 실행 화면 검토가 먼저 가져갔다. 재발주 검토는 **Review 06** 이다.

---

## 5. 겪어 둔 것 (같은 실수를 두 번 하지 않기 위해)

- **전역 색상 회전은 정밀 렌더에서 무너진다.** 보라 미니어처를 적색으로 돌리자 피부가 초록이 됐다.
  색상대(의상·받침대의 색상 ±45° · 채도 0.25 이상)로 제한하면 살아난다
- **전처리가 조립기 수정을 무력화할 수 있다.** 띠를 10:3 으로 채워 두면 마디 길이가 변 전체와
  같아져 언제나 1마디가 된다. 정사각으로 돌려야 의도대로 3마디가 나온다
- **파일 시간을 믿지 않는다.** `채팅방_3` 만 저장 순서가 달랐고, 시간만 믿었으면 조립이 통째로 어긋났다
- **색 치환 임계값은 배경까지 문다.** 배경 `#8b9eba` 와 웹 청색의 거리가 109인데 임계값이 110이었다

---

## 6. 문서 지도

| 무엇 | 어디 |
|---|---|
| 등록 문서 · Studio 담당 범위 | [`brief/PROJECT_BRIEF.md`](brief/PROJECT_BRIEF.md) |
| **판단과 그 이유** | [`reviews/REVIEW_LOG_ivory-frame.md`](reviews/REVIEW_LOG_ivory-frame.md) |
| C1 발주서 | [`orders/2026-08-17_C1-panel.md`](orders/2026-08-17_C1-panel.md) |
| 색 토큰 값 | `reviews/2026-08-17_ivory-palette/tokens.json` |
| 아트 방향 (**정본**) | 게임 저장소 `ART_DIRECTION.md` |
| 자산 목록 (**정본**) | 게임 저장소 `src/art/spec.ts` · `ART_REQUIREMENTS.md` |
| 파이프라인 (**정본**) | 게임 저장소 `ASSET_PIPELINE.md` · `npm run art:check` |
| **게임 쪽에서 본 왕래 기록** | 게임 저장소 `ART_STUDIO_LIAISON.md` — 받은 결정이 그쪽에 만든 상태와 후속 작업 |

> **게임 쪽에 남아 있는 어긋남 하나** — `spec.ts` 의 자산 목록이 B1 이전 모델이라
> `art:check` 는 ★★★ 18장을 세는데 `ART_REQUIREMENTS.md` 는 약 35장이다.
> **그쪽 작업이고 Studio 를 막지 않는다.** 다만 자산 수를 인용할 때 어느 쪽 숫자인지 밝힌다.
