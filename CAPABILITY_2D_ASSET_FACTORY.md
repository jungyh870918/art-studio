# 제작 능력 등록 — 2D Art Factory (`2d-assets`)

작성일: 2026-08-17 · **관측 갱신: 2026-08-17 22:10** (`2d-assets` `65a9daf` 까지)
대상 저장소: `~/Desktop/Projects/2d-assets`

> **§2 · §8 · §9 · §10 은 「그때 관측한 것」이다.** 하루 사이에 양쪽이 다 움직였으므로
> 갱신한 자리를 각 절에 표시해 두었다. **판단(§3~§7 · §11 · §13)은 바뀌지 않았다.**

> **이 문서의 위치.** `STUDIO_USAGE_CONTEXT.md`와 같은 성격의 **얇은 보조 문서**다.
> 새 governance도, 새 lifecycle도, 새 승인 단계도 정의하지 않는다.
> **충돌 시 기존 18개 문서(`CLAUDE.md` · `docs/01`~`11` · `templates/`)가 항상 우선한다.**
>
> 하는 일은 하나다 — `05_TOOL_ROLES.md` §25·§27이 말한 대로
> **하나의 제작 능력을 조건과 함께 기록**하고, Studio와의 경계를 정한다.

---

## 1. 한 줄

> **`art-studio`는 결정하고 기억한다. `2d-assets`는 증폭한다.
> 둘 사이에 상하 관계는 없고, 딱 하나만 위아래가 있다 — 승인.**

---

## 2. 두 저장소는 실제로 무엇인가 (관측된 상태)

두 저장소는 서로의 존재를 모른 채 만들어졌고, **겹치지 않는다.** 겹치기는커녕 정확히 반대쪽 절반이다.

> **2026-08-17 22:10 갱신 — 이 표의 「코드 없음 / 자산 없음」 칸은 하루 만에 낡았다.**

| | `art-studio` | `2d-assets` |
|---|---|---|
| 실체 | 문서 18개 (`CLAUDE.md` + `docs/01`~`11` + `templates/` 6) | 동작하는 Python 파이프라인 + 실제 산출물 |
| 코드 | ~~없다~~ → **생겼다.** `studio/tools/contact_sheet.py` 1종 + 프로젝트 전용 도구 5종(`dice-dominion/tools/`) | `tools/ap2d/` 14모듈 + 팩 adapter · **테스트 200** · Unity C# 10종 + PlayMode 테스트 |
| 자산 | ~~없다~~ → **후보만 있다.** 배경 2 · 패널 부품 3벌. **승인 0** | 원본 PNG 2,141장 (LPC · CC0 · LimeZu · HD Survivor) |
| 입출력 | 프로젝트 3개 등록(`three-kingdoms` · `dice-dominion` · `tteoklak-island`) | **발주 입구** `02_CATALOG/CAPABILITIES.md` · **회신 출구** `order_brief.py` (2026-08-17 추가) |
| 잘하는 것 | 판단의 경계 · 상태의 의미 · 스타일 격리 · 학습 승격 기준 | 결정적 조합 · 팔레트 · 검증 · Unity 배선 |
| 못하는 것 | **스스로 그림을 만들지 못한다** — 생성기 연결이 아직 없다 | **결정하지 못한다.** 「골랐다」를 담을 자리가 없다 (그쪽 README 가 스스로 그렇게 적었다) |

`2d-assets`는 사실상 `03_PROJECT_STRUCTURE.md` §5가 말한 `studio/tools/` · `studio/exporters/` ·
`studio/workflows/`를 **이미 상당 부분 구현해 놓은 것**이다. 다만 그것을 모른 채,
하나의 제작 방식(**licensed modular composition**)에 특화되어 만들어졌다.

---

## 3. 상하 관계에 대한 답 — 딱 한 군데만 위아래가 있다

「어느 쪽이 위인가」는 대체로 잘못된 질문이다. 자원과 명령의 우위는 작업마다 다르다.
다만 **한 가지는 명확한 상하**이고, 그것이 이 문서의 핵심이다.

`2d-assets`에는 **승인이라는 개념 자체가 없다.**

```
01_SOURCE      남의 원본 (읽기 전용)
02_CATALOG     스캔 결과
05_GENERATED   "언제든 삭제하고 재생성 가능 = 버려도 됨"   ← README 규칙 2
06_UNITY_EXPORT 파생 패키지
```

`06_ASSET_LIFECYCLE.md`의 다섯 상태에 얹으면 **`APPROVED`가 통째로 비어 있다.**
`make_contact_sheet.py`가 후보를 나란히 보여주긴 하는데, **고른 결과가 착지할 파일이 없다.**
(비교 대상: `three-kingdoms`에는 `picks-batch-01.txt` → `select.js` → `source/`가 있다.)

→ **그 빈자리가 정확히 `art-studio`가 채우는 것이다.**
`APPROVED`의 의미(`06`), 승인의 절차(`08`), 승인의 기록(`templates/REVIEW_LOG.md`),
그리고 승인된 것이 사는 곳(`03` §13).

그래서 관계는 이렇게 정리된다.

```
디렉터          결정한다
art-studio      그 결정의 의미와 기록을 보관한다      ← 승인은 여기서만 일어난다
2d-assets       결정된 것을 대량으로 만들어 낸다
엔진            그 결과를 실제 화면으로 되돌려준다
```

**「생성됨 + 검증 통과」를 「승인됨」으로 읽지 않는다** (`06` §8 · `08` §2 · `09` §2).
`2d-assets`의 validator가 10종 PASS를 내도 그것은 Technical Pass이지 채택이 아니다.

---

## 4. 가장 큰 위험 — 팩은 스타일을 함께 들고 온다

실제 산출물을 보고 판단한 것이다.
`05_GENERATED/reports/lpc_phase1_population.png` — villager 10명이 나란히 있는데
**머리색이 전부 같은 주황이고 옷이 전부 같은 흰색**이다.

원인은 둘이고, 둘 다 중요하다.

1. **subset이 작다** — Phase 1은 슬롯당 자산이 몇 개뿐이다. 이건 늘리면 해결된다.
2. **팩의 화풍이 고정되어 있다** — 이건 늘려도 해결되지 않는다. LPC는 LPC처럼 보인다.

두 번째가 전략적으로 결정적이다.

> **구매·CC0 팩의 파츠를 그대로 게임에 넣으면, 그 게임의 아트 방향을 팩 제작자가 정한 것이 된다.**

이것은 `11_LEARNING_AND_REUSE.md` §15가 열거한 경고 신호와 정확히 같다 —
「모든 게임이 비슷한 palette를 사용한다 · 같은 캐릭터 비율이 반복된다」.
`2d-assets`를 무비판적으로 쓰면 스튜디오가 그 상태로 직행한다.

**그러나 이것은 `2d-assets`를 쓰지 말라는 뜻이 아니다.**
쓸 것과 쓰지 않을 것을 가르라는 뜻이다 — §6.

---

## 5. 다리 — Factory가 입력에 요구하는 것은 스타일이 아니라 **파츠 계약**이다

`2d-assets`의 기계 장치는 **어떤 화풍인지 전혀 모른다.** 카탈로그가 계산하는 조건만 본다
(`tools/README.md`의 generation capability 표).

```
composable = parts_separable ∧ pre_aligned ∧ animation_compatible
```

즉 파츠가 이 계약만 만족하면 **출처가 LPC든 내가 만든 것이든 같은 코드가 돈다.**
`compose.resolve_layer()`가 물리적 차이(개별 PNG vs 시트)를 흡수하는 유일한 지점이고,
그 뒤는 전부 generic이다. 실제로 CC0(벡터 만화풍)와 LPC(64px 도트)가 이미 같은 파이프라인을 탄다.

**그러므로 전략은 「Factory를 쓸 것인가」가 아니라 「Factory에 무엇을 먹일 것인가」다.**

`2d-assets/CLAUDE.md`가 이미 그 자리를 비워 두었다 —
「Generative(예외): 라이선스가 허용하는 소스 또는 **직접 만든 master asset**에만 적용.
결과물은 `01_SOURCE`가 아니라 **별도 계층**에 둔다.」

### 내 파츠가 만족해야 하는 계약

생성기로 뽑든 손으로 그리든, 파츠를 받을 때 이것을 요구한다.

| 요구 | 왜 |
|---|---|
| 슬롯마다 **별도 PNG** (합쳐진 완성 시트 금지) | `parts_separable`. 시트로 오면 `composed_sheet`로 떨어져 조합 불가 |
| 모든 파츠가 **같은 논리 셀 · 같은 원점** | `pre_aligned` · `shared_cell`. 어긋나면 팔이 몸에서 떨어진다 |
| 애니메이션·프레임 수가 슬롯 간 일치 (또는 `allow_subset` 선언) | `animation_compatible` |
| **z 순서를 선언**할 것 | LPC는 `zPos`로 선언한다. 사람이 베껴 적으면 어긋난다 |
| 색이 **램프 구조**를 따를 것 | 팔레트 교체가 그 위에서만 성립한다 |

이 다섯 줄이 생성 발주서(`templates/ASSET_BRIEF.md`)의 기술 항목이 되고,
동시에 `STYLE_SPEC.md` §8 「Layer 분리 규칙」·「장비 attachment 방식」에 들어갈 값이다.

> **이 계약은 화풍과 무관하다.** 셀레스트 밀도로 만들든 옥토패스 밀도로 만들든 같은 계약이다.
> 그래서 이것은 Studio 공통 지식이고, 화풍은 프로젝트에 남는다 (`11` §3).

---

## 6. 무엇을 Factory에 보내고 무엇을 보내지 않는가

`06_ASSET_LIFECYCLE.md` §20 — 「Lifecycle의 깊이는 자산의 중요도와 불확실성에 비례한다」를
제작 수단 선택에 그대로 적용한다.

| | **모집단 (population)** | **정체성 (identity)** |
|---|---|---|
| 예 | 마을 주민 · 잡몹 · 병사 · 소품 · 가문 색 변형 · 군중 | 주인공 · 보스 · 시그니처 랜드마크 · UI 언어 · 맵 디오라마 · 타이틀 |
| 필요한 것 | **많고 서로 다르고 일관될 것** | **하나가 정확히 그것일 것** |
| 수단 | **`2d-assets`** — 조합 · 팔레트 · seed | 생성기 + 손 + 디렉터 왕복 |
| 판단 깊이 | 계열 규칙을 한 번 승인 (`06` §16) | 자산마다 후보 → 리뷰 → 승인 |
| 후보 제시 | contact sheet | 개별 비교 |

**Factory는 기존 것을 재조합할 뿐 새로운 시각적 정체성을 만들지 못한다.**
모집단의 폭을 넓히는 데는 압도적이고, 정체성을 만드는 데는 쓸 수 없다.
이 한 줄이 두 저장소의 분업 전체를 결정한다.

---

## 7. Source of Truth — 두 저장소가 다른 말을 하고 있다. 이렇게 푼다

`10_ENGINE_HANDOFF.md` §17은 **「두 개의 Source of Truth를 만들지 않는다」**고 못 박았다.
그런데 두 저장소의 어휘가 지금 정면으로 부딪힌다.

```
art-studio   approved/  가 원본이고 exports/ 는 파생물이다        (03 §16 · 06 §9)
2d-assets    05_GENERATED/character.json 이 source of truth 인데
             그 폴더는 "언제든 삭제하고 재생성 가능 = 버려도 됨"    (README 규칙 2)
```

**푸는 방법은 PNG를 승인 대상에서 내리는 것이다.**

Factory는 결정적이다 — 난수 스트림이 `sha256("<rule>|<seed>|<attempt>|<key>")`이고,
validator가 매 실행마다 ① 소스 전체를 sha256으로 다시 확인하고 ② 같은 seed 재생성 결과 일치를
검사한다. 즉 **`(팩 해시, 규칙, seed)`가 같으면 PNG는 바이트 단위로 같다.**

그러므로:

```
APPROVED SOURCE   =  팩 해시 + 규칙 파일 + seed        (텍스트. 몇 줄이다)
EXPORT            =  PNG · 시트 · 프리팹              (언제든 재생성)
```

「디렉터가 seed 4007을 채택했다」가 **그 자체로 완결된 승인 기록**이 된다.
버전 관리도 되고, 용량도 없고, 재현도 보장된다. `06` §9의 원본–파생 방향이 그대로 지켜진다.

> **단, 이 성질은 `01_SOURCE`가 바이트 단위로 그대로일 때만 성립한다.**
> validator의 소스 해시 검사가 바로 그 전제를 지키는 장치다 — 그 검사를 끄면 승인 기록이 무의미해진다.

**손으로 후처리한 파츠가 생기는 순간 이 성질은 깨진다.** 그때는 그 파일이 승인 원본이 되고,
프로젝트의 `approved/`로 올라간다. **어느 쪽인지 헷갈리는 상태를 만들지 않는다.**

---

## 8. 지금 실제로 비어 있는 것 (증거와 함께)

`11_LEARNING_AND_REUSE.md` §9의 「반복이 확인되면 공통화한다」 기준으로 정리한다.

| # | 빈 것 | 증거 | 승격 후보 | 시점 |
|---|---|---|---|---|
| 1 | **승인이 착지할 자리** | Factory에 `picks`/`approved` 개념 없음 — **2026-08-17 그쪽 README 가 「판정하지 않는 것」 절로 그 부재를 명시했다** | `art-studio`의 `approved/` + `REVIEW_LOG` | **지금** |
| 2 | **정확한 램프 스왑** | `ap2d/palette.py`는 multiply tint뿐. LPC 규칙이 「ramp recolor 엔진을 만들지 않았다」고 명시 | `dice_board`의 `swapPalette`/`rampMapping` (이미 있음) | 내 파츠 시작 시 |
| 3 | **「진짜 도트인가」 검사** | Factory에 없음. 지금은 불필요 — LPC는 이미 도트다 | `dice_board`의 `art:pixel` 4종 + `checkDiamondTile` | **생성기를 붙이는 순간** |
| 4 | **생성기 연결** | `tools/` 전체에 네트워크 코드 0줄 (의도된 것 — 라이선스) | own-parts 계층에만 붙인다 | 계층이 생긴 뒤 |
| ~~5~~ | ~~**Studio 코드 자리**~~ | **해소 시작.** `studio/tools/contact_sheet.py` 와 프로젝트 3개가 생겼다 | — | ✅ 2026-08-17 |
| **6** | **생성기 연결 — Studio 쪽** | `three-kingdoms` 가 `providers.js` 로 이미 붙였고, `tteoklak-island` 는 붙어 있지 않아 **사람 손을 기다린다** | `REQUEST_TO_DIRECTOR.md` §7-4 의 「절반만 흡수」 | **디렉터 승인 대기** |

②와 ③이 특히 값진 발견이다 — **두 저장소가 같은 필요를 각각 따로 만났다.**
`dice_board`는 도트 팔레트 스왑과 도트 판정을 이미 만들었고, Factory는 그 둘이 없다.
`11` §9의 「반복이 확인된 뒤 공통화」 조건을 실제로 충족한 유일한 항목들이다.

**다만 지금 옮기지 않는다.** ②는 내 파츠를 넣기 시작할 때, ③은 생성기를 붙일 때 필요해진다.
필요해지기 전에 옮기면 검증되지 않은 방식이 스튜디오 기본값처럼 보인다 (`03` §22).

---

## 9. 냉정하게 — Factory는 지금의 병목을 풀어주지 않는다

세 게임 중 **어느 것도 Factory의 현재 출력을 그대로 쓸 수 없다.**

| 게임 | 필요한 것 | LPC/CC0로 되는가 |
|---|---|---|
| `dice_board` | ~~쿼터뷰 미니어처 · 다이아몬드 타일 · HD-2D~~ → **정밀 렌더 일러스트 · 아이보리 UI 프레임 · Unity 3D 링 위 평면 타일** (2026-08-17 화풍 확정. **도트 방향 철회**) | ✗ **더 멀어졌다** |
| `three-kingdoms` | 95×110 인물 초상 181명 (그중 22명은 원화 → 도트로 이미 나왔다) | ✗ 초상은 modular 캐릭터가 아니다 |
| `secret-of-falling-island` | 일러스트 배경 · 회화체 초상 189×189 | ✗ 도트가 아니다 |

→ **세 게임에 대한 Factory의 값어치는 «팩»이 아니라 «기계»다.** §5의 계약대로 파츠를 먹여야 돈다.

그리고 그 파츠가 아직 없다.

> **2026-08-17 갱신 — `dice_board` 의 「아트 바이블이 병목」은 풀렸다.** 화풍·톤·엔진이 확정됐고
> 그쪽은 「우리가 art-studio 에 막고 있는 것은 없다」고 적었다. **병목이 Studio 쪽으로 옮겨왔다** —
> C1 발주서 v2. 병목의 성질은 그대로다: **정체성 자산 한 벌이 아직 없다.**

> **Factory는 지금 병목(정체성 자산이 없다)을 풀지 못한다.
> 그다음 병목(양산·변형·엔진 배선)을 미리 풀어 둔 것이다.**

이걸 혼동하면 「팩토리를 만들었는데 왜 게임 아트가 안 생기지」가 된다.
반대로 이 순서를 알고 있으면, 정체성 자산 한 벌이 나오는 순간 모집단이 **하루 만에** 나온다.

---

## 10. 순서 — 얇게, 한 게임으로, 실제로 통과시킨다

`07_GENERATION_WORKFLOW.md`의 sample → runtime test → batch를 저장소 통합에 적용한다.
**추상적으로 설계하지 않는다.** 실제로 통과시켜 어디가 쓸리는지 본다.

### Slice 0 — 새 그림 0장으로 지금 할 수 있는 것

> **2026-08-17 22:10 — 1·2 는 끝났고 3 이 남았다.**

1. ✅ **두 저장소가 서로를 알게 한다** — 이 문서 + `2d-assets/00_DOCS/DIRECTOR_CONTEXT.md`.
   그쪽이 발주 입구(`CAPABILITIES.md`)와 회신 출구(`order_brief.py`)까지 열어 두었다
2. ✅ **`art-studio/projects/<game>/`을 하나 만든다** — 셋을 만들었다
   (`three-kingdoms` · `dice-dominion` · `tteoklak-island`). 다만 **`ART_DIRECTION` 이
   `dice-dominion` 에는 없다** — 그 프로젝트는 게임 저장소가 정본을 갖는다고 등록했다
3. ⬜ **own-parts 계층을 합성 파츠로 뚫어 본다** — `01_SOURCE`가 아닌 별도 계층에
   규격에 맞는 가짜 파츠 한 벌을 넣고 scan → rule → generate → contact sheet → Unity export까지 돌린다.
   **여기서 ②(램프 스왑 없음)가 반드시 걸린다. 걸리는 것이 이 슬라이스의 목적이다.**
   → 통과시킨 뒤 **합성 파츠는 반드시 지운다.** 진짜 자산으로 오인되면 그때부터 리포트를 믿을 수 없다

### Slice 1 — 정체성 자산 한 벌이 도착한 뒤

4. 파츠 1종을 §5 계약대로 받아 규칙을 쓰고, **모집단으로 굽는다**
5. contact sheet를 **실제 표시 크기로** 렌더해 디렉터 앞에 놓는다 (`08` §8)
6. 고른 seed를 `REVIEW_LOG`에 적는다 — 그것이 승인이다 (§7)

### Slice 2 — 화면에서 되돌려 받는다

7. consumer package로 게임 프로젝트에 넣고 씬 스크린샷
8. 문제가 보이면 **원인의 층부터 가른다** (`10` §13) — Source / Export / Engine
9. 여기서 반복되는 것만 Studio로 승격한다 (`11` §9)

---

## 11. 하지 않을 것

- **`2d-assets`를 `art-studio` 안으로 옮기지 않는다.** `03` §17이 게임 저장소 흡수를 금한 것과 같은 이유이고,
  게다가 그쪽은 테스트 167개를 가진 살아 있는 코드베이스다. 경로로 연결한다
- **세 게임에 Factory를 복제하지 않는다.** 같은 checker를 세 번 복사하는 것이 `11` §9의 「너무 늦은 공통화」다
- **구매 팩 파츠를 그대로 게임에 싣지 않는다** (§4). 프로토타입·플레이스홀더·기계 검증용은 예외이며,
  그때는 그것이 플레이스홀더임이 드러나 있어야 한다
- **Factory에 「좋은가」 판정을 넣지 않는다.** validator는 사실만 본다.
  (`secret-of-falling-island`가 톤 거리로 합격선을 만들었다가 걷어낸 전례가 있다)
- **`art-studio`의 18개 문서를 지금 고치지 않는다.** 이 문서는 그 아래에 있다
- **구매 에셋을 생성 AI에 입력으로 넣지 않는다.** 대부분의 라이선스가 금지하고,
  `2d-assets`의 게이트가 이미 그것을 전제로 서 있다

---

## 12. 도구가 붙는 자리 (앞선 논의의 연장)

| 도구 | 이 구조에서의 자리 |
|---|---|
| **ChatGPT** | Factory **바깥** — 컨셉·화풍 탐색, 정체성 자산의 시각적 대화. 파츠 계약과 무관한 단계 |
| **도트/픽셀 전문 생성기** | **own-parts 계층**에만 붙는다. `01_SOURCE`에는 절대 쓰지 않는다 (라이선스) |
| **Claude Code** | `art-studio` 규약 유지 · Factory 규칙 작성 · 검증 · 승격 · 엔진 배선 |
| **MCP** | 탐색 구간의 배선 — 뽑기 → 검사 → 프롬프트 수정을 한 자리에서. 대량은 스크립트 |
| **Unity / Godot** | 판정 자리. Factory의 consumer package가 그 입력을 만든다 |
| **사람** | 파츠 마감 · 최종 채택. `05` §19대로 정상적인 제작 방식이다 |

---

## 13. 핵심

> **`art-studio`는 결정하고 기억한다. `2d-assets`는 증폭한다. 상하는 승인에만 있다.**
>
> **Factory의 입력 요구는 화풍이 아니라 파츠 계약이다** — 그래서 내 파츠를 그 계약에 맞추면
> 남의 팩 없이도 같은 기계가 돈다.
>
> **팩은 스타일을 함께 들고 온다.** 모집단은 Factory로, 정체성은 디렉팅 루프로 가른다.
>
> **결정적 생성 덕분에 승인은 PNG가 아니라 `(팩 해시 + 규칙 + seed)`에 걸 수 있다.**
>
> **Factory는 지금의 병목을 풀지 않는다.** 정체성 자산 한 벌이 나온 뒤의 병목을 미리 풀어 둔 것이다.
>
> **공통화는 지금 하지 않는다.** 램프 스왑은 내 파츠를 넣을 때, 도트 판정은 생성기를 붙일 때 필요해진다.
